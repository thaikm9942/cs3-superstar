#ifndef __POWERUP_H__
#define __POWERUP_H__

#include <stdbool.h>
#include "body.h"
#include "sdl_wrapper.h"

// Defines BodyTypes
typedef enum {
    EXPAND,
    INVINCIBILITY,


} PowerType;

// Defines a body_info struct to be passed in Body
void expand_platform(Body* platform, ){

}

#endif // #ifndef __SHAPE_H__
